package com.vibestcg.scoretracker.ui.theme

import androidx.compose.ui.graphics.Color

// MARK: Pudgy Penguins Brand Colors

// Icy light blue (penguin theme)
val PenguinBlue = Color(0xFF99D9FF)

// Deeper arctic blue
val ArcticBlue = Color(0xFF66A6E6)

// Vibrant purple (vibes theme)
val VibesPurple = Color(0xFFB380FF)

// Warm orange/coral accent
val PenguinOrange = Color(0xFFFF9966)

// Deep ocean blue for backgrounds
val OceanDeep = Color(0xFF1A2633)

// Lighter ocean for cards/panels
val OceanMid = Color(0xFF263340)

// Success green
val IceGreen = Color(0xFF66E6B3)

// Danger/warning red
val AlertRed = Color(0xFFFF4D4D)

// UI Colors
val CardBackground = OceanMid
val PrimaryAccent = PenguinBlue
val SecondaryAccent = VibesPurple
val WarningColor = PenguinOrange
